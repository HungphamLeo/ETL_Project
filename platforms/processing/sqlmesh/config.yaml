gateways:
  local_duckdb:
    connection:
      type: duckdb
      database: ./stock_lakehouse.db
      # NOTE: pre_statements bị remove — field này không tồn tại ở một số version SQLMesh.
      # S3/MinIO config được inject qua Python sau khi Context khởi tạo.
      # Xem: platforms/processing/sqlmesh/sqlmesh_engine.py → _configure_s3()

default_gateway: local_duckdb

model_defaults:
  dialect: duckdb
  owner: data_engineering_team
