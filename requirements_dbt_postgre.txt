# dbt core
dbt-core==1.7.15

# dbt adapter for PostgreSQL
dbt-postgres==1.7.15

# optional nhưng rất nên có
psycopg2-binary==2.9.9

# yaml / jinja (dbt dùng nội bộ, pin để tránh conflict)
PyYAML==6.0.1
Jinja2==3.1.4
