{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run test notebook as script",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/notebooks/test.py",
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}",
      "env": {
        "PYTHONPATH": "${workspaceFolder}"
      }
    }
  ]
}
